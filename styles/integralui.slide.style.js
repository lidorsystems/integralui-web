import { c as css } from '../external/lit-element.js';

const iuiSlideDefaultStyle = css`
    .iui-slide {
        background: var(--slide-background, white);
        float: left;
    }
`;

export { iuiSlideDefaultStyle };
